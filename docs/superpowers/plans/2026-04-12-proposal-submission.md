# Proposal Submission Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finalize the proposal milestone deliverables and commit a clean, professional repo structure ready for Milestone 01 development.

**Architecture:** All proposal files are already written. This plan creates the project directory skeleton (so the repo looks complete to a hiring manager), adds a `.env.example` documenting required secrets, and commits everything with a meaningful message.

**Tech Stack:** git, GitHub

---

## File Map

| Action | Path | Purpose |
|---|---|---|
| Already exists | `docs/job-posting.pdf` | Proposal deliverable |
| Already exists | `docs/proposal.md` | Proposal deliverable |
| Already exists | `CLAUDE.md` | Project context for Claude Code |
| Already exists | `.gitignore` | Credential + template exclusions |
| Create | `etl/.gitkeep` | Placeholder for extraction scripts |
| Create | `dbt/.gitkeep` | Placeholder for dbt project |
| Create | `dashboard/.gitkeep` | Placeholder for Streamlit app |
| Create | `knowledge/raw/.gitkeep` | Placeholder for scraped sources |
| Create | `knowledge/wiki/.gitkeep` | Placeholder for wiki pages |
| Create | `knowledge/index.md` | Knowledge base index (stub) |
| Create | `.env.example` | Documents required secrets |

---

### Task 1: Create project directory skeleton

**Files:**
- Create: `etl/.gitkeep`
- Create: `dbt/.gitkeep`
- Create: `dashboard/.gitkeep`
- Create: `knowledge/raw/.gitkeep`
- Create: `knowledge/wiki/.gitkeep`

- [ ] **Step 1: Create all placeholder directories**

```bash
mkdir -p etl dbt dashboard knowledge/raw knowledge/wiki
touch etl/.gitkeep dbt/.gitkeep dashboard/.gitkeep knowledge/raw/.gitkeep knowledge/wiki/.gitkeep
```

- [ ] **Step 2: Verify structure looks right**

```bash
find . -not -path './.git/*' -not -path './.DS_Store' | sort
```

Expected output includes:
```
./CLAUDE.md
./dashboard/.gitkeep
./dbt/.gitkeep
./docs/job-posting.pdf
./docs/proposal.md
./etl/.gitkeep
./knowledge/raw/.gitkeep
./knowledge/wiki/.gitkeep
```

---

### Task 2: Create knowledge base index stub

**Files:**
- Create: `knowledge/index.md`

- [ ] **Step 1: Write the knowledge index stub**

Create `knowledge/index.md` with this content:

```markdown
# Knowledge Base Index

This index lists all wiki pages in `knowledge/wiki/` with one-line summaries.
Pages are generated by Claude Code from scraped sources in `knowledge/raw/`.

## Wiki Pages

| Page | Summary |
|---|---|
| [overview.md](wiki/overview.md) | Gap Inc. at a glance: brand portfolio, market position, recent strategic shifts |
| [brands-and-competitors.md](wiki/brands-and-competitors.md) | Brand positioning, target customers, and competitive landscape |
| [media-and-marketing-themes.md](wiki/media-and-marketing-themes.md) | Patterns across sources: marketing events, search drivers, press themes |

## Raw Sources

Raw scraped documents are stored in `knowledge/raw/`. Sources include:
- Gap Inc. investor relations / press releases (gapinc.com)
- Business of Fashion articles
- Retail Dive coverage

*Wiki pages will be populated in Milestone 02 as sources are scraped and ingested.*
```

---

### Task 3: Create `.env.example`

**Files:**
- Create: `.env.example`

- [ ] **Step 1: Write `.env.example`**

Create `.env.example` with this content:

```bash
# Snowflake credentials
SNOWFLAKE_ACCOUNT=your_account_identifier
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_DATABASE=your_database
SNOWFLAKE_WAREHOUSE=your_warehouse
SNOWFLAKE_ROLE=your_role

# Copy this file to .env and fill in your values
# Never commit .env to git
```

- [ ] **Step 2: Confirm `.env` is in `.gitignore`**

```bash
grep "^\.env$" .gitignore
```

Expected: `.env` (already present from initial commit)

---

### Task 4: Commit all proposal deliverables

- [ ] **Step 1: Stage all new and modified files**

```bash
git add CLAUDE.md docs/proposal.md .gitignore .env.example \
        etl/.gitkeep dbt/.gitkeep dashboard/.gitkeep \
        knowledge/raw/.gitkeep knowledge/wiki/.gitkeep knowledge/index.md
```

- [ ] **Step 2: Verify staged files look right**

```bash
git status
```

Expected: all files above listed under "Changes to be committed". No `.env`, no `docs/proposal-template.md`.

- [ ] **Step 3: Commit**

```bash
git commit -m "$(cat <<'EOF'
feat: add proposal, CLAUDE.md, and project directory structure

- docs/proposal.md: Gap Inc. Analyst Media Analytics proposal
- CLAUDE.md: project context, star schema, knowledge base query conventions
- .env.example: documents required Snowflake secrets
- etl/, dbt/, dashboard/, knowledge/: directory skeleton for full project

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Push to GitHub**

```bash
git push origin main
```

- [ ] **Step 5: Verify repo is public and files are visible**

Open https://github.com/nquek/analyst-media-analyst in a browser and confirm:
- `CLAUDE.md` is visible at the root
- `docs/proposal.md` is present
- Directory structure (etl, dbt, dashboard, knowledge) is visible
